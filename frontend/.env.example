# Leave unset for Vite's local /api proxy, or set this to the deployed API origin.
VITE_API_URL=""
# Used only by `npm run dev` when the API runs on another local port.
VITE_API_PROXY_TARGET="http://localhost:4000"
